@model CourseManagement.DAL.Entites.Course
@{
    ViewBag.Title = "Delete Course";
}

<h2>Delete Course</h2>
<h4>Are you sure you want to delete this?</h4>

<div>
    <strong>@Model.Name</strong> - @Model.Category
    <br />
    Instructor: @Model.Instructor.FirstName @Model.Instructor.LastName
</div>

<form asp-action="Delete">
    <input type="hidden" asp-for="Id" />
    @* <button type="submit" asp-action="Index" class="btn btn-danger">Delete</button> *@
    <input type="submit" value="Save" class="btn btn-primary" />

    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>
﻿
